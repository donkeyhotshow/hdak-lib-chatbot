```text
🚀 ПОЛНИЙ АУДИТ + ФІКС БАГІВ + ДИЗАЙН v3.0 — HDAK Chatbot

═══ КРИТИЧНИЙ АУДИТ ВСІХ БАГІВ (знайти + виправити) ═══

1. BROWSER CONSOLE ERRORS (DevTools → Console):
```
[X] React warnings (key, hydration)
[X] Unhandled Promise rejections  
[X] Network 4xx/5xx (API failures)
[X] Service Worker errors
[X] PWA manifest warnings
```

2. MOBILE BUGS (375px viewport):
```
[X] Text overflow / clipping
[X] Keyboard covers input
[X] Touch targets <44px
[X] iOS zoom на focus
[X] Safe area insets (notch)
```

3. API + DB ERRORS:
```
[X] /api/chat/health → 500 або повільно >3s
[X] DB connection timeouts
[X] Missing env vars (Forge/Anthropic)
[X] SQL injection (prepared statements)
[X] Rate limiting відсутній
```

4. PWA + OFFLINE:
```
[X] Service Worker не реєструється
[X] manifest.json invalid
[X] Install prompt не показується
[X] Offline cached responses
[X] Background sync
```

═══ ДИЗАЙН 10/10 POLISH ═══

1. TYPOGRAPHY SYSTEM (DM Sans):
```css
:root {
  --font-title: 28px/1.2;    /* H1 */
  --font-h2: 22px/1.3;       /* H2 */
  --font-body: 16px/1.5;     /* Text */
  --font-caption: 13px/1.4;  /* Chips */
  --line-height-tight: 1.3;
  --line-height-normal: 1.5;
}
```

2. SPACING TOKENS (8pt grid):
```css
--spacing-xs: 0.5rem;  /* 8px */
--spacing-sm: 0.75rem; /* 12px */
--spacing-md: 1rem;    /* 16px */
--spacing-lg: 1.5rem;  /* 24px */
--spacing-xl: 2rem;    /* 32px */
```

3. COMPONENT REFRESH:
```
.ChatBubble:
  border-radius: 20px 20px 20px 4px;
  shadow: 0 4px 12px rgba(92,58,30,0.15);
  backdrop-filter: blur(10px);

.ActionChip:
  min-h-11 min-w-20
  border-radius: 25px
  shadow-sm hover:shadow-md
  transition: all 0.2s cubic-bezier(0.4,0,0.2,1)
```

4. MICROANIMATION SYSTEM:
```css
@keyframes bubbleIn {
  from { opacity: 0; transform: translateY(12px) scale(0.95); }
  to { opacity: 1; transform: translateY(0) scale(1); }
}
@keyframes typingPulse {
  0%,100% { opacity: 1; }
  50% { opacity: 0.7; }
}
```

═══ PERFORMANCE 100/100 ═══

1. CODE SPLITTING:
```
next.config.js:
```js
splitChunks: {
  chunks: 'all',
  cacheGroups: {
    vendor: { test: /[\\/]node_modules[\\/]/ }
  }
}
```
```

2. CRITICAL RENDER PATH:
```
app/layout.tsx:
<link rel="preload" href="/fonts/DM-Sans.woff2" as="font">
<link rel="dns-prefetch" href="v0-hdak-lib-chatbot.vercel.app">
```

═══ ACCESSIBILITY WCAG AAA ═══

1. FOCUS MANAGEMENT:
```tsx
useEffect(() => {
  inputRef.current?.focus()
}, [])
```

2. ARIA LIVE REGIONS:
```tsx
<div 
  role